import { VersionNewComponent } from './version-new/version-new.component';
import { Routes } from '@angular/router';

export const VERSION_ROUTES:Routes = [
    {
        path: 'new',
        component: VersionNewComponent        
    },
]
